#include <stdio.h>

bool Palin(int binf, int bsup, char ch) {
    if (binf >= bsup)
    {
        return true;
    } else {
        int *bi, *bs;
        if (ch[bi] == ch[bs])
        {
            return Palin(binf+1,bsup-1,ch);
        } else if(ch[bi] == " ") {

        }
        
    }
    
}